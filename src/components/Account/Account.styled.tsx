import styled from 'styled-components';

const AccountStyled = styled.article`
  width: 300px;
  margin: 0 auto;
  justify-content: center;
`;

const AccountTitle = styled.h1``;

const AccountImg = styled.img``;

const AccountResultItem = styled.div``;

export { AccountStyled, AccountTitle, AccountImg, AccountResultItem };
